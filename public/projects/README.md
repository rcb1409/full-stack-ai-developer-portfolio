# Project images

Place project images here. Filenames must match the `imageFile` value in `constants.tsx`.

Current expected files:

- `neuralvision-dashboard.png` — NeuralVision Dashboard
- `echostream-saas.png` — EchoStream SaaS
- `wolfpack-connect.png` — Wolfpack Connect

Use `.png` or `.jpg`; update `imageFile` in `constants.tsx` if you use a different extension (e.g. `neuralvision-dashboard.jpg`).
